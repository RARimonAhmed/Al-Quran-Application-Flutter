import 'package:flutter/material.dart';

class MediaData extends StatelessWidget {
  const MediaData({
    super.key,
    // this.imageUrl, this.title, this.artist
  });
  // final imageUrl, title, artist;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text("Rimon"),
      ],
    );
  }
}
